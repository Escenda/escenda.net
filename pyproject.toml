[tool.poetry]
name = "project"
version = "0.1.0"
description = ""
authors = ["Escenda <contact@escenda.net>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
django = "^4.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
